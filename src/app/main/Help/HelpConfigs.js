import Help from './Help';

const HelpConfig = {
    settings: {
        layout: {}
    },
    routes: [
        {
            path: "/help",
            component: Help
        }
    ]
};

export default HelpConfig;